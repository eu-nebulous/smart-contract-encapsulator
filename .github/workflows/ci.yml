name: CI
on:
  push:
    branches:
      - main
      - r[1-9]
  pull_request:
    branches:
      - main
      - r[1-9]
jobs:
  build-and-push-smart-contract-encapsulator:
    name: Build and push smart-contract-encapsulator
    uses: eu-nebulous/github-actions/.github/workflows/build-and-push-container-image.yml@main
    with:
      context: nebulous-sce-application
      image-name: smart-contract-encapsulator
    secrets: inherit
